package ${package};

import me.shedaniel.autoconfig.ConfigData;
import me.shedaniel.autoconfig.annotation.Config;

@Config(name = ${modName}.ID)
public final class ${modName}ConfigData implements ConfigData {

    private boolean enabled = true;
}

package ${package}

import me.shedaniel.autoconfig.ConfigData
import me.shedaniel.autoconfig.annotation.Config

@Config(name = ${modClassName}.ID)
class ${modConfigDataClassName} : ConfigData {

    private var enabled: Boolean = true
}
